name: Build Studio Multi-Project

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/supabase-studio-multi

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout this repo (patches)
        uses: actions/checkout@v4
        with:
          path: patches-repo

      - name: Shallow clone supabase/supabase
        run: |
          git clone --depth=1 --filter=blob:none \
            https://github.com/supabase/supabase.git supabase-src
          cd supabase-src
          git sparse-checkout init --cone
          git sparse-checkout set \
            apps/studio \
            packages/ui \
            packages/common \
            packages/config \
            packages/shared-data \
            packages/icons \
            packages/ai-commands \
            patches
          git checkout

      - name: Create empty patches dir if missing
        run: mkdir -p supabase-src/patches

      - name: Apply our source patches
        run: |
          cp -rv patches-repo/patches/. supabase-src/

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-

      - name: Build and push (amd64)
        uses: docker/build-push-action@v6
        with:
          context: supabase-src
          file: supabase-src/apps/studio/Dockerfile
          build-args: |
            NEXT_PUBLIC_IS_PLATFORM=false
          target: production
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
